"use strict";

// ternary operator

// let age = 18;

// let message = age >= 18 ? "You can drive" : "You can't drive";

// console.log(message);



// multiple condition in ternary operator
let speed = 50;

let message = speed >= 120 ? "Too Fast" : speed >= 80 ? "Fast" : "Slow";

console.log(message);